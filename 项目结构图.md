# NetGeoCourier2 - 项目完整结构图

> **生成日期**: 2026-04-11  
> **项目名称**: NetGeoCourier2 (网络地理快递)  
> **项目类型**: Android 应用 (Kotlin + Jetpack Compose)  
> **架构模式**: MVVM + Repository Pattern

---

## 📐 一、项目层级结构

```
NetGeoCourier2/
│
├── 📦 构建配置层
│   ├── build.gradle.kts                    # 根项目构建脚本
│   ├── settings.gradle.kts                 # 项目模块设置
│   ├── gradle.properties                   # Gradle 全局属性
│   ├── local.properties                    # 本地配置（API 密钥、SDK 路径）
│   ├── gradle/
│   │   ├── libs.versions.toml              # 版本目录（统一依赖版本管理）
│   │   └── wrapper/
│   │       └── gradle-wrapper.properties   # Gradle 包装器版本
│   ├── gradlew                             # Gradle Wrapper (Unix/Linux)
│   ├── gradlew.bat                         # Gradle Wrapper (Windows)
│   └── .gradle/                            # Gradle 缓存目录（自动生成）
│
├── 📱 应用模块 (app/)
│   ├── app/build.gradle.kts                # 应用模块构建配置
│   ├── app/proguard-rules.pro             # ProGuard 混淆规则
│   │
│   └── 📁 app/src/main/                   # 主源代码目录
│       │
│       ├── 📄 AndroidManifest.xml         # 应用清单文件
│       │
│       ├── 📁 java/com/example/netgeocourier/   # Kotlin 源文件
│       │   │
│       │   ├── 🎯 MainActivity.kt                    # 主活动（应用入口）
│       │   ├── 🚀 SplashActivity.kt                  # 启动页活动
│       │   │
│       │   ├── 📊 data/                              # 数据层（Models）
│       │   │   ├── EvaluationModels.kt        # 评估 API 响应模型
│       │   │   └── NetTestResult.kt           # 网络测速结果数据类
│       │   │
│       │   ├── 🔧 helper/                            # 工具类/服务层
│       │   │   ├── AuthTokenStore.kt          # JWT 令牌存储（SharedPreferences）
│       │   │   ├── CoordTransform.kt          # WGS-84 ⇄ GCJ-02 坐标转换
│       │   │   ├── FileHelper.kt              # CSV/HTML 文件导出
│       │   │   ├── LocationHelper.kt          # 高德定位服务 ⚠️ 有 BUG
│       │   │   └── SpeedTestHelper.kt         # SpeedTest CLI 调用封装
│       │   │
│       │   ├── 🌐 network/                           # 网络层
│       │   │   ├── ApiClient.kt               # Retrofit + OkHttp 客户端配置
│       │   │   ├── AuthInterceptor.kt         # Bearer Token 认证拦截器
│       │   │   ├── EvaluationApiService.kt    # 评估 API 接口定义
│       │   │   └── EvaluationRepository.kt    # 评估数据仓库（单例）
│       │   │
│       │   ├── 🎨 screen/                            # UI 层（Compose 屏幕）
│       │   │   ├── EvaluationScreen.kt        # 评估结果展示页面
│       │   │   └── NetTestScreen.kt           # 主测速界面
│       │   │
│       │   ├── 🎭 ui/theme/                          # Compose 主题系统
│       │   │   ├── Color.kt                   # 颜色定义（Material 3）
│       │   │   ├── Theme.kt                   # 主题配置（暗色/亮色）
│       │   │   └── Type.kt                    # 字体/排版定义
│       │   │
│       │   └── 🧠 viewmodel/                         # ViewModel 层
│       │       └── NetTestViewModel.kt        # 测速业务逻辑（状态管理）
│       │
│       └── 📁 res/                                   # Android 资源
│           │
│           ├── 📁 drawable/                          # 可绘制资源
│           │   ├── ic_launcher_background.xml   # 应用图标背景
│           │   ├── ic_launcher_foreground.xml   # 应用图标前景
│           │   └── test.jpg                     # 测试图片
│           │
│           ├── 📁 layout/                            # XML 布局文件
│           │   └── activity_splash.xml          # 启动页布局
│           │
│           ├── 📁 mipmap-anydpi/                     # 自适应图标（XML）
│           │   ├── ic_launcher.xml
│           │   └── ic_launcher_round.xml
│           │
│           ├── 📁 mipmap-hdpi/                       # 高密度图标 (1.5x)
│           │   ├── ic_launcher.webp
│           │   └── ic_launcher_round.webp
│           │
│           ├── 📁 mipmap-mdpi/                       # 中密度图标 (1.0x)
│           │   ├── ic_launcher.webp
│           │   └── ic_launcher_round.webp
│           │
│           ├── 📁 mipmap-xhdpi/                      # 超高密度图标 (2.0x)
│           │   ├── ic_launcher.webp
│           │   └── ic_launcher_round.webp
│           │
│           ├── 📁 mipmap-xxhdpi/                     # 超超高密度图标 (3.0x)
│           │   ├── ic_launcher.webp
│           │   └── ic_launcher_round.webp
│           │
│           ├── 📁 mipmap-xxxhdpi/                    # 超超高密度图标 (4.0x)
│           │   ├── ic_launcher.webp
│           │   └── ic_launcher_round.webp
│           │
│           ├── 📁 values/                            # 默认值资源
│           │   ├── colors.xml                   # 颜色调色板
│           │   ├── strings.xml                  # 字符串（中文）
│           │   └── themes.xml                   # 应用主题
│           │
│           ├── 📁 values-en/                         # 英文语言资源
│           │   └── strings.xml                  # 英文字符串
│           │
│           └── 📁 xml/                              # XML 配置文件
│               ├── backup_rules.xml             # 自动备份规则
│               ├── data_extraction_rules.xml    # 数据提取规则
│               └── file_paths.xml               # FileProvider 路径配置
│
└── 🌐 后端服务 (campus-net-backend/)
    └── ...（Node.js + Express 服务，待进一步探索）
```

---

## 📊 二、文件统计

### 2.1 Kotlin 源文件分布

| 目录/包 | 文件数 | 文件列表 | 功能分类 |
|---------|--------|----------|----------|
| `根目录` | 2 | MainActivity.kt, SplashActivity.kt | 应用入口 |
| `data/` | 2 | EvaluationModels.kt, NetTestResult.kt | 数据模型 |
| `helper/` | 5 | AuthTokenStore.kt, CoordTransform.kt, FileHelper.kt, LocationHelper.kt, SpeedTestHelper.kt | 工具服务 |
| `network/` | 4 | ApiClient.kt, AuthInterceptor.kt, EvaluationApiService.kt, EvaluationRepository.kt | 网络通信 |
| `screen/` | 2 | EvaluationScreen.kt, NetTestScreen.kt | UI 界面 |
| `ui/theme/` | 3 | Color.kt, Theme.kt, Type.kt | 主题样式 |
| `viewmodel/` | 1 | NetTestViewModel.kt | 状态管理 |
| **总计** | **19** | - | - |

### 2.2 资源文件分布

| 类型 | 数量 | 说明 |
|------|------|------|
| `.xml` | 12 | 布局、配置、值资源 |
| `.webp` | 10 | 应用图标（5 个密度 × 2 个变体） |
| `.jpg` | 1 | 测试图片 |
| **总计** | **23** | - |

### 2.3 配置文件

| 文件 | 用途 |
|------|------|
| `AndroidManifest.xml` | 权限、组件注册、API Key |
| `local.properties` | 本地 API 密钥（不提交 Git） |
| `build.gradle.kts` | 依赖声明、编译配置 |
| `libs.versions.toml` | 统一版本管理 |
| `gradle.properties` | Gradle 全局属性 |
| `proguard-rules.pro` | 代码混淆规则 |

---

## 🔄 三、数据流向图

### 3.1 定位功能数据流

```
用户点击 "开始定位"
    ↓
NetTestScreen (UI)
    ↓ 调用
LocationHelper.getCurrentLocation()
    ↓
检查权限 (PermissionHelper)
    ↓ 授权？
┌─────┴─────┐
│ 是        │ 否
│           ↓ 显示提示
│  AMapLocationClient.create()
│       ↓
│   setLocationListener(this)
│       ↓
│   setLocationOption(高精度/单次)
│       ↓
│   startLocation()
│       ↓
│   [等待回调] ←───────── 高德定位 SDK
│       ↓
│   onLocationChanged(location)
│       ↓
│   stopLocation() + unregisterLocationListener ⚠️
│       ↓
│   locationContinuation.resume(AMapLocation)
│       ↓
└──→ NetTestViewModel
        ↓
    更新 UI (NetTestScreen)
        ↓
    显示坐标、精度、地址
```

**⚠️ 问题点标记：**
- `unregisterLocationListener` 拼写错误（应为全小写）
- 缺少重复调用防护

---

### 3.2 网络测速数据流

```
用户点击 "开始测速"
    ↓
NetTestScreen (UI)
    ↓ 调用
SpeedTestHelper.runSpeedTest()
    ↓
执行 SpeedTest CLI (子进程)
    ↓
解析 JSON 输出
    ↓
创建 NetTestResult 对象
    ↓
保存到 List<NetTestResult>
    ↓
更新 ViewModel 状态
    ↓
Compose 重新渲染
    ↓
显示：下载/上传/Ping 结果
```

---

### 3.3 文件导出数据流

```
用户点击 "导出 CSV"
    ↓
NetTestScreen (UI)
    ↓ 调用
FileHelper.saveCsv()
    ↓
获取测速结果列表
    ↓
⚠️ 错误：调用 CoordTransform.wgs84ToGcj02()
    ↓（高德坐标已经是 GCJ-02，再次转换导致偏差）
写入 CSV 文件
    ↓
提示用户保存路径
```

---

### 3.4 评估页面数据流

```
用户点击 "打开评估"
    ↓
NetTestScreen → EvaluationScreen (页面切换)
    ↓
EvaluationScreen (Compose)
    ↓ init
EvaluationRepository.getEvaluation()
    ↓
调用 EvaluationApiService.getEvaluation()
    ↓
ApiClient 创建 Retrofit 实例
    ↓
AuthInterceptor 注入 Token
    ↓
GET /api/v1/evaluation (后端)
    ↓
后端验证 Token + 计算评估
    ↓
返回 JSON {
  score, level, metrics, suggestions
}
    ↓
Repository 解析数据
    ↓
更新 UI 显示结果
```

---

## 🏗️ 四、架构分层图解

```
┌─────────────────────────────────────────────────────────────────┐
│                        Presentation Layer                       │
│  ┌─────────────────┐              ┌─────────────────┐           │
│  │  NetTestScreen  │              │ EvaluationScreen│           │
│  │  (Compose UI)   │              │  (Compose UI)   │           │
│  └────────┬────────┘              └────────┬────────┘           │
│           │                                │                    │
│           └──────────────┬─────────────────┘                    │
│                          │                                      │
│         ┌────────────────▼────────────────┐                     │
│         │       MainActivity              │                     │
│         │   (State: currentPage)          │                     │
│         └────────────────┬────────────────┘                     │
└──────────────────────────┼─────────────────────────────────────┘
                           │
┌──────────────────────────┼─────────────────────────────────────┐
│                    Domain Layer (ViewModel)                     │
│         ┌────────────────▼────────────────┐                     │
│         │   NetTestViewModel              │                     │
│         │   - MutableState<UIState>       │                     │
│         │   - 业务逻辑                    │                     │
│         │   - 调用 Helper/Network         │                     │
│         └────────────────┬────────────────┘                     │
└──────────────────────────┼─────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
         ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  Helper      │  │  Network     │  │  Data        │
│  (Services)  │  │  (API)       │  │  (Models)    │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ LocationHelper│ │ ApiClient    │ │ NetTestResult│
│ SpeedTestHelper│ │ AuthInterceptor│ │ EvaluationModels│
│ FileHelper    │ │ EvaluationRepo│ └──────────────┘
│ CoordTransform│ └──────────────┘
└──────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                    External Libraries                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 高德定位 SDK  │  │ Retrofit     │  │ Jetpack      │      │
│  │  (AMap SDK)  │  │  + OkHttp    │  │  Compose     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ SpeedTest    │  │ Kotlinx      │  │ AndroidX    │      │
│  │   CLI        │  │  Coroutines  │  │  Lifecycle  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 五、关键文件详解

### 5.1 应用入口

#### `MainActivity.kt`
```kotlin
class MainActivity : ComponentActivity() {
    lateinit var viewmodel: NetTestViewModel
    private lateinit var locationHelper: LocationHelper

    override fun onCreate(savedInstanceState: Bundle?) {
        // 初始化 ViewModel
        viewmodel = ViewModelProvider(this)[NetTestViewModel::class.java]

        // 初始化定位助手
        locationHelper = LocationHelper(this)

        // 设置 Compose 内容
        setContent {
            var currentPage by rememberSaveable { mutableStateOf(AppPage.TEST) }

            NetGeoCourierTheme {
                when (currentPage) {
                    AppPage.TEST -> NetTestScreen(...)
                    AppPage.EVALUATION -> EvaluationScreen(...)
                }
            }
        }

        requestPermissions()
    }
}
```
**职责：**
- 管理页面状态（TEST / EVALUATION）
- 初始化 ViewModel 和 LocationHelper
- 请求运行时权限

---

### 5.2 定位服务

#### `LocationHelper.kt` ⚠️ **有严重 Bug**

```kotlin
class LocationHelper(private val context: Context) : AMapLocationListener {
    private var locationClient: AMapLocationClient? = null
    private var locationContinuation: Continuation<AMapLocation?>? = null

    suspend fun getCurrentLocation() = suspendCancellableCoroutine { cont ->
        locationClient = AMapLocationClient(context.applicationContext).apply {
            setLocationListener(this@LocationHelper)
            setLocationOption(createLocationOption())
            startLocation()
        }
    }

    override fun onLocationChanged(location: AMapLocation?) {
        stopLocation()  // ← 这里会调用
        // 处理定位结果
    }

    fun stopLocation() {
        locationClient?.stopLocation()
        locationClient?.unRegisterLocationListener(this)  // ❌ 拼写错误！
        locationClient = null
    }
}
```
**⚠️ 已知问题：**
1. 第 142 行：`unRegisterLocationListener` → 应为 `unregisterLocationListener`
2. 缺少重复调用防护
3. 双重坐标转换问题（间接影响）

---

### 5.3 坐标转换

#### `CoordTransform.kt`

```kotlin
object CoordTransform {
    fun wgs84ToGcj02(lat: Double, lon: Double): Pair<Double, Double> {
        // WGS-84 → GCJ-02 转换算法
        // 适用于 GPS 原始坐标转火星坐标
    }
}
```
**⚠️ 使用注意：**
- 高德定位 SDK 返回的坐标**已经是 GCJ-02**
- 如果对高德坐标再次调用此函数 → **双重转换 → 坐标严重偏差**

---

### 5.4 文件导出

#### `FileHelper.kt`

```kotlin
object FileHelper {
    fun saveCsv(context: Context, results: List<NetTestResult>) {
        val (gcjLat, gcjLon) = CoordTransform.wgs84ToGcj02(
            it.latitude, it.longitude
        )  // ❌ 错误：高德坐标已是 GCJ-02

        // 写入 CSV...
    }
}
```
**⚠️ Bug：** 双重转换导致 CSV 文件坐标错误。

---

### 5.5 网络层

#### `ApiClient.kt`
```kotlin
object ApiClient {
    fun getClient(): Retrofit {
        return Retrofit.Builder()
            .baseUrl(BuildConfig.API_BASE_URL)
            .addConverterFactory(GsonConverterFactory.create())
            .client(okHttpClient)
            .build()
    }
}
```

#### `AuthInterceptor.kt`
```kotlin
class AuthInterceptor : Interceptor {
    override fun intercept(chain: Interceptor.Chain): Response {
        val token = AuthTokenStore.getAccessToken(context)
        val request = if (token != null) {
            chain.request().newBuilder()
                .addHeader("Authorization", "Bearer $token")
                .build()
        } else chain.request()
        return chain.proceed(request)
    }
}
```

---

## 🔗 六、依赖关系矩阵

| 模块 | 依赖的模块 | 依赖的库 |
|------|-----------|----------|
| **MainActivity** | ViewModel, LocationHelper, PermissionHelper | AndroidX Activity, Compose |
| **NetTestScreen** | ViewModel, FileHelper, SpeedTestHelper | Compose UI, Material 3 |
| **NetTestViewModel** | LocationHelper, SpeedTestHelper | ViewModel, Coroutines |
| **LocationHelper** | PermissionHelper | 高德定位 SDK |
| **ApiClient** | AuthInterceptor | Retrofit, OkHttp, Gson |
| **EvaluationRepository** | ApiClient, EvaluationApiService | - |
| **FileHelper** | CoordTransform | - |
| **CoordTransform** | - | Kotlin math |

---

## 🎨 七、UI 组件树

```
NetGeoCourierTheme (Material 3 Theme)
    │
    ├─── TEST Page (NetTestScreen)
    │    │
    │    ├─── TopAppBar
    │    │    └── "网络测速"
    │    │
    │    ├─── 定位信息卡片
    │    │    ├── 纬度: 39.9042°N
    │    │    ├── 经度: 116.4074°E
    │    │    └── 精度: 10m
    │    │
    │    ├─── 测速按钮
    │    │    └── "开始测速"
    │    │
    │    ├─── 测速结果卡片
    │    │    ├── 下载速度: 85.2 Mbps
    │    │    ├── 上传速度: 23.1 Mbps
    │    │    └── Ping: 28 ms
    │    │
    │    ├─── 导出按钮
    │    │    ├── "导出 CSV"
    │    │    └── "导出 HTML 地图"
    │    │
    │    └─── 评估入口按钮
    │         └── "打开网络评估 →"
    │
    └─── EVALUATION Page (EvaluationScreen)
         │
         ├─── TopAppBar
         │    ├── "网络评估"
         │    └── Back 按钮
         │
         ├─── 评分卡片
         │    ├── 分数: 78
         │    └── 等级: good
         │
         ├─── 指标卡片
         │    ├── 下载平均: 82.5 Mbps
         │    ├── Ping 平均: 28 ms
         │    ├── RSSI 平均: -61 dBm
         │    └── SNR 平均: 24 dB
         │
         ├─── 建议列表
         │    └── LazyColumn
         │         ├── "建议 1: ..."
         │         └── "建议 2: ..."
         │
         └─── 记录数
              └── "共 5 条记录"
```

---

## 🔑 八、权限清单

### AndroidManifest.xml 声明的权限

| 权限 | 用途 | API Level |
|------|------|-----------|
| `ACCESS_FINE_LOCATION` | 高精度 GPS 定位 | 31+ |
| `ACCESS_COARSE_LOCATION` | 粗略网络定位 | 31+ |
| `INTERNET` | 访问后端 API | - |
| `WRITE_EXTERNAL_STORAGE` | 导出 CSV/HTML | ≤ 29 |
| `READ_EXTERNAL_STORAGE` | 读取导出文件 | ≤ 29 |
| `POST_NOTIFICATIONS` | 测速完成通知 | 33+ |

### 运行时权限请求

在 `PermissionHelper.kt` 中动态请求：
```kotlin
PermissionHelper.registerPermissionLauncher(activity) {
    // 权限授予后执行
}
```

---

## 🔐 九、密钥配置

### local.properties

```properties
sdk.dir=C:\\Users\\kevinmou\\AppData\\Local\\Android\\Sdk
AMAP_WEB_KEY=0fe0d875041a90a5686e62f931c5f991   # 高德 Web 服务 Key
AMAP_ANDROID_KEY=6565f23f22c878d0f4409f15df6fac71  # 高德 Android SDK Key
```

**⚠️ 安全注意：** `local.properties` 不应提交到 Git（已在 `.gitignore` 中）。

---

## 🏃 十、运行流程

### 启动流程

```
1. 用户启动应用
   ↓
2. Android 系统加载 SplashActivity
   ↓
3. SplashActivity 显示 2 秒 → 跳转 MainActivity
   ↓
4. MainActivity.onCreate()
   ├─ 初始化 ViewModel
   ├─ 初始化 LocationHelper
   ├─ 设置 Compose Content
   └─ 请求权限
   ↓
5. 用户授予权限（或拒绝）
   ↓
6. 显示 NetTestScreen（主界面）
   ↓
7. 用户可执行：
   ├─ 定位（点击"获取位置"）
   ├─ 测速（点击"开始测速"）
   ├─ 导出数据（CSV/HTML）
   └─ 查看评估（跳转 EvaluationScreen）
```

---

## 🐛 十一、已知 Bug 位置速查

| 文件名 | 行号 | 问题描述 | 严重程度 |
|--------|------|----------|----------|
| `LocationHelper.kt` | 142 | `unRegisterLocationListener` 拼写错误 | 🔴 Critical |
| `FileHelper.kt` | 35 | 双重坐标转换 | 🔴 Critical |
| `NetTestScreen.kt` | 352-353 | 双重坐标转换 | 🔴 Critical |
| `NetTestScreen.kt` | 413-414 | 双重坐标转换 | 🔴 Critical |
| `LocationHelper.kt` | 62-86 | 缺少重复初始化防护 | 🟡 High |
| `LocationHelper.kt` | 124-127 | 错误信息未传递 | 🟢 Medium |

---

## 📦 十二、依赖库完整列表

### AndroidX
- `androidx.core:core-ktx:1.10.1`
- `androidx.lifecycle:lifecycle-runtime-ktx:2.6.1`
- `androidx.lifecycle:lifecycle-viewmodel-ktx:2.6.1`
- `androidx.lifecycle:lifecycle-extensions:2.2.0`
- `androidx.lifecycle:lifecycle-viewmodel-compose:2.6.2`
- `androidx.activity:activity-compose:1.8.0`
- `androidx.fragment:fragment-ktx:1.6.2`
- `androidx.compose.ui:ui`
- `androidx.compose.ui:ui-graphics`
- `androidx.compose.ui:ui-tooling`
- `androidx.compose.ui:ui-tooling-preview`
- `androidx.compose.ui:ui-test-junit4`
- `androidx.compose.material3:material3`

### 第三方库
- `com.amap.api:location:6.2.0` (高德定位)
- `com.squareup.retrofit2:retrofit:2.11.0`
- `com.squareup.retrofit2:converter-gson:2.11.0`
- `com.squareup.okhttp3:okhttp:4.12.0`

### 测试库
- `junit:junit:4.13.2`
- `androidx.test.ext:junit:1.1.5`
- `androidx.test.espresso:espresso-core:3.5.1`

---

## 🎓 十三、设计模式

| 模式 | 应用位置 | 说明 |
|------|----------|------|
| **MVVM** | 整体架构 | Model-View-ViewModel 分离关注点 |
| **Repository** | EvaluationRepository | 数据源抽象 |
| **Singleton** | ApiClient, AuthTokenStore | 全局唯一实例 |
| **Observer** | ViewModel + Compose State | 响应式 UI 更新 |
| **Listener** | LocationHelper (AMapLocationListener) | 回调处理 |
| **Interceptor** | AuthInterceptor | OkHttp 请求拦截 |
| **Factory** | AMapLocationClient.create() | 对象创建 |

---

## 📖 十四、相关文档

所有详细分析文档均在项目根目录：

```
D:\NetGeoCourier2\
├── 定位失败问题分析与解决方案.md          ⚠️ 高德定位 Bug 分析
├── 谷歌转高德定位问题分析与修复.md        ⚠️ 迁移问题报告
├── 高德定位迁移指南.md                    📚 迁移步骤
├── LOCATION_FIX.md                       🔧 定位修复指南
├── MVVM_REFACTOR_COMPLETE.md             ✅ MVVM 重构完成
├── README_EVALUATION.md                  📝 评估页面集成文档
└── 项目完整分析报告.md                    📘 本综合报告
```

---

## ✅ 十五、检查清单

### 环境检查
- [ ] JDK 版本降级到 11 或 17
- [ ] `JAVA_HOME` 环境变量正确配置
- [ ] Android Studio 已安装
- [ ] Gradle 同步成功

### 代码修复
- [ ] 修复 `unRegisterLocationListener` 拼写错误
- [ ] 修复双重坐标转换（3 处）
- [ ] 添加重复初始化防护
- [ ] 增强错误处理（可选）

### 功能验证
- [ ] 应用成功编译
- [ ] 应用成功安装到设备/模拟器
- [ ] 定位功能正常
- [ ] 测速功能正常
- [ ] CSV 导出坐标正确
- [ ] 评估页面可访问

---

**文档生成完成** ✅  
**下一步：** 按照《项目完整分析报告.md》中的修复建议逐步修复问题。
